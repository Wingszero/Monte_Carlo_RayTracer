You can change the field of KDTREE to control the max_depth of KdTree in cornell_dragon.txt.The default value is 12.

The sample image used less than 3 secs to render with Kd-Tree.

When max-depth set to 0, it means the mesh only has a bounding box, which will cost serveral minutes to render. 
